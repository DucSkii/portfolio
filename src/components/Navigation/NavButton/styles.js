import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles({
  buttons: {
    maxWidth: '60px',
    minWidth: '60px',
    height: '60px',
    padding: '0',
    margin: '0',
  },
})